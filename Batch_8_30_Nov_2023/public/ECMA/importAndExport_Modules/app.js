import {count, maxvalues, showInfo, username} from './sampleLib.js';
import maxSal from './salutil.js';

console.log("value of count is " + count);

console.log("value of maxvalues is " + maxvalues);

showInfo();

console.log(maxSal);